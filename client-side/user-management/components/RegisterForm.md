# RegisterForm

**File:** `src/components/User/Auth/RegisterForm.tsx`

Registration form with name, username, email, mobile, age, password.

## Features
- All required fields validated
- Calls userService.register()
- Stores JWT in localStorage
- Redirects to home on success
